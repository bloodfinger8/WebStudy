<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="userSQL">
 	<insert id="write" parameterType="user.bean.UserDTO">
 		insert into usertable values(#{name},#{id},#{pwd})
 	</insert>
 	<delete id="delete" parameterType="java.lang.String" >
 		delete from usertable where id = #{id}
 	</delete>
 	<select id="getList" resultType="user.bean.UserDTO"> <!-- UserDTO안에다가 담는다 -->
 		select * from usertable
 	</select>
 	<select id="select" resultType="user.bean.UserDTO">
 		select * from usertable where id = #{id}
 	</select>
 	<select id="update" parameterType="user.bean.UserDTO">
 		update usertable set name = #{name} , pwd = #{pwd} where id = #{id}
 	</select>
</mapper> 